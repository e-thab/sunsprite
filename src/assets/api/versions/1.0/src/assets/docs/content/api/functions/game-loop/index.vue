<script lang="ts">
export const meta = {
	title: 'Game Loop',
	icon: 'tabler:clock-play',
	summary: 'Functions that run code every frame, on a delay, or on a condition.',
	order: ['forever', 'repeat', 'repeat-until', 'repeat-while', 'after', 'every', 'when'],
}

export default {}
</script>

<template>
	<DocSection id="content-1">
		<p>The core scheduling functions almost every script is built on — from the primary forever() loop to one-shot delays and conditional triggers.</p>
	</DocSection>
</template>
